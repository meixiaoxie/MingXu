// This file provides the stable import path used by model-independent code.
// The actual contract lives with the other shared runtime types.
export type {
  ModelInput,
  ModelOutput,
  ModelProvider,
} from "../core/types.js";
